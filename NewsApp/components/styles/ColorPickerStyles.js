import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    color: {
        width: 60,
        height: 60,
        marginHorizontal: 15,
        borderRadius: 10
    },
    general: {
        flex: 1
    }
});

export default Styles;